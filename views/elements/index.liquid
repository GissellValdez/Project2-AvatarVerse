<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AvatarVerse</title>

    <style type="text/css">
        body {
            background-color: red;
            color: rgb(215, 247, 236);
            text-align: left;
            font-size: 18;
        }

        .container {
            display: grid;
            grid-template-columns: repeat(3, auto);
            justify-items: center;
        }
    </style>
</head>

<body>
    <div id="bannerDiv">
        <h1>Elements</h1>
        <a href="/">Back to main page</a><br>
    </div>

    <br>
    <br>
    <div class="container">
        {%for element in elements%}
        <div class="elementImg">
            <h3>{{element.name}}</h3>
            <a href="/elements/{{ element._id }}">
                <img src="{{element.img}}" width="75" height="100">
            </a>
            <br>
            <br>
        </div>
        {%endfor%}
    </div>
</body>

</html>